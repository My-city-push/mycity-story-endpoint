{
  "name": "mycity-story-endpoint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node publish-once.js",
    "check": "node --check index.js && node --check publish-once.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "firebase-admin": "^13.4.0"
  }
}
